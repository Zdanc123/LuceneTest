@model IEnumerable<WebApplication2.Models.Character>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
         <thead>
        <form asp-controller="People" asp-action="Index"vocab="" method="get">
            
                <p>
                    Title: <input type="text" name="SearchString">
                    <input type="submit" value="Filter" />
                </p>
            
        </form>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                
                <th></th>
            </tr>
    </thead>

        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CharacterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CharacterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CharacterID })
        </td>
    </tr>
}

</table>
